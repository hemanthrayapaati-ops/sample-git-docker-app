pipeline:
  build:
    image: node:18
    commands:
      - echo "🚀 Running build pipeline"
      - node --version
      - echo "✅ Build complete!"

  docker:
    image: docker:latest
    commands:
      - echo "🐳 Simulating Docker build..."
      - sleep 3
      - echo "✅ Docker build complete!"
